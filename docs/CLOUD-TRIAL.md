# Cloud trial: one new retrospective issue

For a new manually launched trial, begin with [START-CLOUD.md](START-CLOUD.md). It records the first attempt's pushed work and failures, adds Issue -058 to the exclusions, and uses a fresh branch so the original trial is preserved. The fixed branch name below describes the original assignment.

Andrew requested a trial in ChatGPT Work Cloud before switching weekly production away from the Mac. Use the existing ChatGPT project named **hazel** for the trial. The desktop project with the same name is a separate local folder connection; it is not being converted or deleted.

## Outcome

Produce one new, reviewable retrospective Week in Hazel issue entirely in the cloud, with an illustrated PDF, readable HTML edition, source evidence, authentic screenshots, generated art, editable production files, and proposed ledger entries. Keep it unpublished during the trial. Do not create, enable, or modify a recurring schedule from the trial task.

Production repository: https://github.com/disconcision/hazel-weekly

Reporting repository: https://github.com/hazelgrove/hazel

Start with `AGENTS.md`, `README.md`, and the current `output/week-in-hazel/PRODUCTION.md`, `EDITORIAL.md`, `AUDIENCE.md`, `NUMBERING.md`, `issues.json`, `image-ledger.json`, and `web/README.md`. Inspect the approved Issue 001 illustrated PDF and light Cards stamp exemplar. Earlier revisions explain history; current guides govern production.

## Verify the cloud tools, then proceed

Begin with a brief capability check in the actual cloud task. Determine whether you can retrieve the production files and Hazel Git/GitHub evidence, run code and install the rendering dependencies, operate a browser and save native PNG captures, generate and edit images using references, render/inspect PDFs, and export durable results. Determine separately whether authenticated GitHub writes are available. A successful public repository read does not prove publishing access.

Use the hosted environment and its connected tools. Nothing may depend on a Mac filesystem path, local preview server, desktop browser session, or the local `gh` login. Fix routine path and dependency portability problems in the cloud checkout where feasible, retaining a reproducible setup record. Check a hosted preview first for screenshots; build a historical revision in the cloud when necessary. If an essential capability is unavailable, report the exact failed operation and the smallest concrete setup action required, while retaining useful completed work. Do not silently replace required images with placeholders or claim a full production trial succeeded with missing stages.

## Produce the issue

Choose an interesting, substantial uncovered Friday-to-Friday UTC week in the two years preceding September 11, 2026. Use the historical activity census as a starting point and investigate period code and dated discussions. Exclude the existing issues 001, -019, -060 and -100. Calculate its fixed calendar number using `NUMBERING.md`; the trial's production date must not advance the weekly coverage cutoff.

Aim for roughly 7–11 pages, according to the week's substance. Write concrete, useful reporting for Hazel contributors. Verify historical merge destinations and feature readiness, distinguish experiments from landed work, and avoid importing later outcomes into the historical week. Keep source maps and the original evidence.

Include a fresh cover, a short original fictional comic when it serves the issue, and compelling feature screenshots. Stage disposable code, probes, or rich literals as needed. Preserve genuine high-resolution PNG captures; publishable screenshot treatments should follow the restrained light Cards stamp exemplar, with no added frames. Retain both the authentic capture and the treated illustration, and inspect the latter for altered code or values. Do not repeat an old cover or comic merely because it is convenient.

Render and inspect every PDF page. The HTML edition should retain the finished article content, open with the complete unframed cover illustration, and use the established red AI disclosure banner. Keep previous PDFs, issue URLs, and published files intact. File-size optimization is deferred and must not displace this trial.

## Return reviewable evidence

Deliver the PDF and HTML/assets package, editable sources and setup changes, source map, raw/selected research, original and treated screenshots, prompts, proposed ledger updates, and a short cloud-production QA record. Record the input repository revision, reporting window, historical source heads, tools actually used, and each stage that passed or failed.

If authenticated repository writes are available, save the trial on a separate `codex/cloud-back-issue-trial` branch without merging into `main`. Otherwise provide downloadable artifacts and patches and explicitly identify persistence as unfinished. Do not alter the live personal website in this trial. The editor can review the candidate and integrate it afterward.

## Schedule handover, after the trial

The existing local automation is `week-in-hazel-weekly-zine`, Fridays at 9 a.m. America/Los_Angeles. It remains the only active weekly publisher during the trial.

After the cloud result and persistence/publication path pass review, reconcile the latest ledgers and configure the replacement cloud schedule in a paused state where supported. Pause the local automation and verify that it is inactive before enabling the replacement. Confirm that only one weekly production schedule is active. If the cloud setup fails, leave it disabled; do not run two schedules as a fallback. No schedule changes are part of this trial task.
